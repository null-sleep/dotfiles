# Font (matches iTerm2: Hack Nerd Font Mono, size 15)
font_family      Hack Nerd Font Mono
font_size        15.0

# Window (matches iTerm2: 125x25)
remember_window_size  no
initial_window_width  125c
initial_window_height 25c
window_padding_width  4
hide_window_decorations no
confirm_os_window_close 0

# Cursor (matches iTerm2: bar cursor)
cursor_shape beam

# macOS
macos_option_as_alt yes
macos_quit_when_last_window_closed yes
macos_titlebar_color background

# Tab bar — shows cwd and git branch (like iTerm2 titlebar)
# tab_bar_edge top
# tab_bar_style powerline
# tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title} {fmt.fg._5765F2}({tab.active_wd.replace('/Users/dhruvjauhar', '~')}){fmt.fg.tab}"

# Shell
shell_integration enabled

# Theme (override by creating ~/.config/kitty/current-theme.conf)
# To pick a theme interactively:  kitten themes


# BEGIN_KITTY_THEME
# Dracula
include current-theme.conf
# END_KITTY_THEME