# Halp-specific shell configuration
# Sourced conditionally from .zshrc_config.zsh

# mise: per-project tool versions (elixir, python, node, …) — activated by
# `antigen bundle mise` in .zshrc_config.zsh (the oh-my-zsh plugin runs
# `mise activate zsh` itself and generates completions); no explicit eval
# here or it runs twice per shell.
